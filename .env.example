# Used by docker-compose.yml for the personal Datadog Agent container.
# Get this from your own org: Organization Settings > API Keys.
# This is deliberately separate from the corporate host Agent's key —
# see README.md "Running with Docker" for why.
DD_API_KEY=
